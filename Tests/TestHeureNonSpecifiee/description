On lance le programme avec le flag -t mais sans argument (alors qu'il faudrait qu'elle soit '-t 16' par exemple). On verifie donc si le programme informe l'utilisateur qu'il y a une erreur dans sa commande.
